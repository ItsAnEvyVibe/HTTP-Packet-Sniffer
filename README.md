# HTTP Packet Sniffer
## 🛡️ Overview
A Python-based network analyzer that captures live HTTP traffic. This project demonstrates how data travels over unencrypted connections and highlights the importance of HTTPS.

## 🚀 Features
* Real-time packet interception.
* URL extraction from HTTP headers.
* Raw data parsing to identify potential sensitive information.

## 🛠️ Requirements
* Python 3.x
* Scapy library (`pip install scapy`)
